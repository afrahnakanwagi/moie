specVersion: "v1beta"
serviceId: "moviehub"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "moviehub-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
